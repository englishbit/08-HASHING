#include<iostream>
using namespace std;
#define MAX 10000000
int hash[MAX];
//logic for insertion
void insert(int x){
    int indx=x%MAX;

        while(hash[indx]!=-1){
            indx=(indx+1)%MAX;
        }
        hash[indx]=x;
    
}
int main(){
    
    for(int i=0;i<MAX;i++){
        hash[i]=-1;
    }
    int n;
    //logic for taking input and calling insert function
    cin>>n;
    for(int i=0;i<n;i++){
        int x;
        cin>>x;
        insert(x);
    }








    return 0;

}

